[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "oor"
version = "0.1.0"
description = "operational optimization research project"
dependencies = [
    "numpy>=2.3.5",
    "pandas>=2.3.3",
]
